{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "python run.py",
    "start": "waitress-serve run_waitress:app",
    "test": "pytest",
    "coverage:run": "coverage run -m pytest",
    "coverage:report": "coverage report",
    "lint": "pylint --recursive=y . backend/*",
    "install": "pip install -r requirements.txt"
  }
}
